'use client';
export { SerwistProvider } from '@serwist/next/react';

// Note: For Turbopack, use '@serwist/turbopack/react' instead
// The build uses webpack (--webpack flag) which works with @serwist/next/react
